# BMS 多协议仿真与测试工作台 V1.0 (使用说明书)

## 一、 软件概述

**BMS Protocol Simulator** 是专为储能逆变器研发与测试部门打造的单文件便携式测试上位机。无需安装任何 Python 环境或第三方运行库，双击即可在 Windows 7 / 10 / 11 操作系统上原生运行。

### 核心特性
1. **多协议原生支持**：
   - **CVTE (Modbus RTU)**：支持读取 12 个保持寄存器（`0x0020` ~ `0x002B`），与 3KU/VMH 逆变器底层 100% 精确对齐。
   - **GROWATT (Modbus RTU)**：支持 15 个寄存器（`0x0014` ~ `0x0022`）。
   - **VOLTRONIC (Modbus RTU)**：支持三阶段轮询（`0x0030`, `0x0060`, `0x0070`）。
   - **PYLONTECH (RS485 ASCII)**：支持 `~20...46...` 帧协议，自动计算开尔文温度与反码累加校验。
2. **双工作模式**：
   - **从机模拟模式 (测逆变器)**：模拟电池包实时响应逆变器的轮询请求，支持全 ID（01~15）秒级极速响应。
   - **主机轮询模式 (测电池包)**：上位机作为 Modbus 主机，主动按周期向真实电池包发送查询指令并解析实时回包。
3. **安全提交与回滚机制 (Commit & Rollback)**：
   - **确认生效**：修改模拟量后，必须按下 **`Enter`** 键或 **`Ctrl+S`** 快捷键，数值才会正式下发并更新，数值框变为**绿色高亮提示 2 秒**。
   - **自动回滚**：若用户选中国数值框修改后，**未按确认**直接点击其他区域或控件，该数值框将自动取消选中并**回滚恢复为上一次已提交的数值**，杜绝误触。
4. **自定义默认值与一键恢复 (持久化)**：
   - 点击 **`⚙ 默认值设置`** 可永久自定义 10 项参数的默认基准，自动保存至本地配置文件，下次打开软件依然成立。
   - 点击 **`恢复默认`** 可一键将所有参数快速重置并提交。

---

## 二、 硬件接线指南

### 1. 逆变器 BMS 口 (RJ45 网口) 典型线序
* **Pin 1**: RS485-B (D-) 或 RS485-A
* **Pin 2**: RS485-A (D+) 或 RS485-B
* **Pin 3 / Pin 6 / Pin 8**: GND (地线)

### 2. 接线步骤
1. 将 USB 转 RS-485 转换器的 **A (D+)** 连接至逆变器 BMS 网口的 **RS485-A**。
2. 将 USB 转 RS-485 转换器的 **B (D-)** 连接至逆变器 BMS 网口的 **RS485-B**。
3. （推荐）连接 GND 地线以消除共模电平干扰。

---

## 三、 使用操作步骤

1. **打开软件**：
   * 双击运行桌面的 `BMS_Protocol_Simulator.exe`。
2. **选择通信参数**：
   * **端口**：点击下拉列表选择对应的 COM 口（若未找到点击“刷新”）。
   * **波特率**：默认 `9600`。
   * **BMS协议**：选择 `CVTE (Modbus RTU)`。
   * **模式**：测试逆变器选择 `从机模拟(测逆变器)`。
   * **应答策略**：勾选 `应答全部ID(01~15)`。
3. **建立连接**：
   * 点击 **`打开串口`** 按钮，状态显示 `● 运行中`。
4. **修改数值与测试**：
   * 在模拟量输入框中输入目标值后，**按 `Enter` 或 `Ctrl+S` 提交**，数值框闪烁绿色 2 秒，逆变器即刻同步响应；
   * 若不小心改错数值，直接点击空白处即可自动回滚。
